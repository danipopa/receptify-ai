<template>
  <section class="lp-cta-banner">
    <div class="lp-container lp-cta-inner">
      <div class="lp-eyebrow" style="background: rgba(255,255,255,.15); color: #e0e7ff; margin: 0 auto 24px;">
        <div class="lp-eyebrow-dot" style="background: #fff; box-shadow: 0 0 6px #fff;"></div>
        Get started today
      </div>
      <h2 class="lp-section-title" style="margin-bottom: 16px;">
        Stop missing calls.<br>Start converting them.
      </h2>
      <p class="lp-section-sub">
        Join the businesses already using Receptify to handle every call,
        capture every lead, and delight every caller.
      </p>
      <div class="lp-cta-buttons">
        <NuxtLink to="/register" class="btn btn-white btn-lg">
          Start free — no credit card →
        </NuxtLink>
        <a href="mailto:hello@receptify.us" class="btn btn-ghost-white btn-lg">
          Talk to founders
        </a>
      </div>
    </div>
  </section>
</template>
